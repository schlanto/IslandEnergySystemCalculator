import { describe,expect,it } from 'vitest'
import { checkCompatibility } from '../src/calculation/core'
import type { Component,SelectedComponent } from '../src/models/types'
const source={type:'measurement' as const,title:'Test',url:'https://example.com',accessed:'2026-07-16'};const quality={level:'independently_measured' as const,confidence:'high' as const};const selected=(id:string):SelectedComponent=>({instanceId:id,componentId:id,quantity:1,usage:{mode:'always'}})
const battery:Component={schemaVersion:'1.0',id:'battery',manufacturer:'T',model:'B',name:'Battery',category:'storage',roles:['storage'],storageType:'battery',description:'',electrical:{nominalVoltageV:48},operation:{},sources:[source],notes:[],dataQuality:quality}
const inverter=(max:number):Component=>({schemaVersion:'1.0',id:'inverter',manufacturer:'T',model:'I',name:'Inverter',category:'converter',roles:['converter'],converterType:'dc_ac_inverter',description:'',electrical:{input:{type:'dc',minimumVoltageV:42,maximumVoltageV:max},output:{type:'ac',nominalVoltageV:230,continuousPowerW:3000,peakPowerW:6000}},operation:{},sources:[source],notes:[],dataQuality:quality})
describe('compatibility',()=>{it('accepts a 48 V battery in a 42–60 V range',()=>expect(checkCompatibility([{component:battery,selected:selected('battery')},{component:inverter(60),selected:selected('inverter')}]).find(x=>x.code==='battery-inverter-voltage')?.status).toBe('compatible'));it('rejects incompatible voltage',()=>expect(checkCompatibility([{component:battery,selected:selected('battery')},{component:inverter(45),selected:selected('inverter')}]).find(x=>x.code==='battery-inverter-voltage')?.status).toBe('incompatible'))})
