import { describe,expect,it } from 'vitest'
import { batteryCurrent,batteryRuntimeHours,continuousLoad,interpolateEfficiency,startupLoad } from '../src/calculation/core'
import type { Component,SelectedComponent } from '../src/models/types'
const consumer=(continuous:number|null,startup:number|null):Component=>({schemaVersion:'1.0',id:'test-load',manufacturer:'Test',model:'Load',name:'Load',category:'consumer',roles:['consumer'],description:'',consumerType:'test',electrical:{continuousPowerW:continuous,startupPowerW:startup},operation:{},sources:[{type:'measurement',title:'Test',url:'https://example.com',accessed:'2026-07-16'}],notes:[],dataQuality:{level:'independently_measured',confidence:'high'}})
const selected:SelectedComponent={instanceId:'1',componentId:'test-load',quantity:1,usage:{mode:'always'}}
describe('core calculations',()=>{it('calculates continuous and startup power',()=>{const items=[{component:consumer(500,2000),selected}];expect(continuousLoad(items)).toBe(500);expect(startupLoad(items)).toBe(2000)});it('calculates battery current and runtime',()=>{expect(batteryCurrent(3000,48,.9)).toBeCloseTo(69.44,2);expect(batteryRuntimeHours(5000,500,.9)).toBe(9)});it('interpolates converter efficiency',()=>expect(interpolateEfficiency([{loadPercent:10,efficiency:.84},{loadPercent:50,efficiency:.94}],30)).toBeCloseTo(.89));it('preserves unknown values',()=>expect(continuousLoad([{component:consumer(null,null),selected}])).toBe(0))})
